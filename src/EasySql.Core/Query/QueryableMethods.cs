namespace EasySql.Query
{
    public static class QueryableMethods
    {
        //public static MethodInfo Where = typeof(Queryable).GetMethod("Where", 1, new[] { typeof(IQueryable<>).MakeGenericType(types[0]), typeof(Expression<>).MakeGenericType(typeof(Func<,>).MakeGenericType(types[0], typeof(bool))) });

        //private static Type GetMethod(string name, Action<Type[]> types)
        //{
        //    typeof(Queryable).GetMethod(name, 1, types());
        //}
    }
}
